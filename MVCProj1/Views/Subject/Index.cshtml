@model IEnumerable<MVCProj1.Models.Subjectmodel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "AddSubject")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.subid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.subname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.subid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.subname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cid)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditDetails", new {  id=item.subid }) |
            @Html.ActionLink("Details", "Details", new {  id=item.subid }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.subid })
        </td>
    </tr>
}

</table>
